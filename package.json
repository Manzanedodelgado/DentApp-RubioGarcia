{
  "name": "rubio-garcia-dental",
  "version": "1.0.0", 
  "description": "Sistema de gestión dental con consentimientos automáticos",
  "main": "backend/server.py",
  "scripts": {
    "start": "python -m uvicorn backend.server:app --host 0.0.0.0 --port $PORT",
    "dev": "python -m uvicorn backend.server:app --reload",
    "build": "cd frontend && yarn build",
    "install-frontend": "cd frontend && yarn install",
    "install-backend": "pip install -r backend/requirements.txt"
  },
  "keywords": ["dental", "clinic", "management", "appointments", "consents"],
  "author": "Rubio García Dental",
  "license": "Private"
}