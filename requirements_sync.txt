# Dependencias para sync_modified.py
# Instalar con: pip install -r requirements_sync.txt

# Base (ya las tienes en tu script actual)
pyodbc>=4.0.35
requests>=2.28.0

# Nuevas dependencias para Google Sheets API
gspread>=5.12.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Opcional: Para mejor manejo de fechas
python-dateutil>=2.8.0